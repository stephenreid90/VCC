name: Deploy Web Playground

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests before deploy
        run: pytest tests/ -v
      - name: Deploy to quanticsai.com
        run: echo "Deploy step — configure SSH/rsync/Kubernetes here"
        # TODO: replace with actual deploy command, e.g.:
        # rsync -avz --exclude='.git' . user@quanticsai.com:/var/www/playground/valuations/
