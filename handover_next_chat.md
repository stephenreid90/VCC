# Handover — copy/paste helpers

*Regenerated by Claude at the end of each session. Open this file, select all, copy.*

---

## 1. Commands to run in your terminal

```
cd C:\Users\steph\vcc-valuations
git add -A
git commit -m "UI: DNL rich workbook (DCF+discount+comps/beta/stats/charts) + cfgs path fix"
git push origin main
```

(The sandbox can't finalise git commits — the mount blocks deleting `.git` lock files — so
the commit + push run from your own terminal. Local main is also ~22+ commits ahead of origin.)

---

## 2. Opener to paste into the next chat

Continuing the VCC scenario-valuation UI. Read WORKING_NOTES.md top sections first. Prototypes
generate from ui_prototypes/_generator/ (build_cfgs.py + beta_data.py + gen_ui.py) — always edit
those, then run `python3 build_cfgs.py && python3 gen_ui.py`, never hand-edit the HTML. The
generator now reads/writes a script-local `cfgs_gen.json` (not /tmp), so the old /tmp permission
collision is gone.

Done to date: the Excel download is a full, formula-driven workbook (self-contained, offline, built
in-browser from live state incl. your edits). WBC/CSL get the simple book (Assumptions + one formula
tab per scenario + Summary). **DNL now gets the rich book** (gated on a `richbook` cfg flag): (1)
Assumptions; (2) Comparables & Beta — Hamada unlever/relever, subject β = median of selected levered
(1.10, UI default), a 26-point regression with native SLOPE/RSQ/SE(β)/t(β vs 1), and a scatter chart
with trendline; (3) Discount rate — Re 9.80% → WACC 8.624%, β linked from the comps median; (4) a DCF
to equity per scenario — explicit 5-yr FCFF → EV → net-debt bridge → per share, reduced-form-consistent
so it reconstructs EV and ties EXACTLY to the UI headline (MT EV 8,064 → 3.59; all six tie, Δ 0.0000);
(5) Summary. Verified via LibreOffice recalc + an end-to-end re-run from the shipped HTML CFG. Not
opened in real Excel here — please eyeball the DNL download.

Two things I want next, in order:
1. **WBC and CSL rich templates.** WBC is a bank archetype — cost of equity, ROE fade to Ke, CET1-binding
   payout, no EV bridge (a dividend/excess-return build, not FCFF→EV). CSL is FCFF at cost of equity with
   USD→AUD at 0.66. Mirror the DNL structure where it fits; give each its own archetype template. Flip
   their `richbook` flag on once built.
2. **Real EODHD data behind the β workbench** — replace the beta_data.py mock in the same JSON shape
   (needs Ben's feed / a sample payload). This also feeds the DNL Comparables & Beta sheet.

Hybrid design reminder: the DCF is a reduced-form-consistent reconstruction (F1 solved so the explicit
stream reproduces the anchor×ratio EV), NOT the audited 3-statement model — but each sheet's layout
mirrors the real dnl_muddle_through_valuation_v4.xlsx, so when the DCF engine (M1+) lands its per-scenario
projection lines swap into the same cells. Same mock-now/real-later pattern as the β workbench.

Engine/writer state to know: three shared JS modules are inlined verbatim into gen_ui.py —
`xlsx_writer.js` (→ VCCXLSX; store-zip + OOXML + native scatter charts), `vcc_book.js` (→ VCCBOOK; simple
book, apostrophe/edge-char safe), `dnl_rich.js` (→ DNLRICH; the rich book). `vccDownload` branches on
`CFG.richbook && CFG.beta`. Clean copies live in /tmp/xw/ and are node + LibreOffice tested — reuse them.

Watch the quirks: the Edit/Write tools truncate/corrupt large files (the Edit tool corrupted gen_ui.py on
a one-line change this session) — build large edits sandbox-side and `cp`, keep a clean /tmp copy. And the
`.git` lock quirk — commits finalise from my terminal.

Please keep track of interactions on this chat — I want to swap to a new chat before 20.

---

## 3. How to reuse this

Ask me to "update the handover file" any time and I'll refresh sections 1 and 2 with the latest state —
then you just open this file and copy.
